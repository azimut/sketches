/x 500 def
/y 700 def
/numbers [ 0 1 2 3 1 3 3 ] def
/pick { numbers rand numbers length mod get } def

23 srand

/Courier 15 selectfont x y moveto

1000 {
  pick {
    dup 0 eq { /x x 5 add def pop exit } if % →
    dup 1 eq { /x x 5 sub def pop exit } if % ←
    dup 2 eq { /y y 5 add def pop exit } if % ↑
    dup 3 eq { /y y 5 sub def pop exit } if % ↓
  } loop
  x y moveto
  (?) show
} repeat
